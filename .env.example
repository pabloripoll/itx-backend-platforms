# REMOVE COMMENTS WHEN COPY THIS FILE AND TRIM TRAILING WHITESPACES
# Ask the team for recommended values

# DOCKER VARIABLES FOR AUTOMATION
SUDO=sudo                                               # <- how local user executes system commands - leave it empty if not necessary ----------------------> #
DOCKER=sudo docker                                      # <- how local user executes Docker commands --------------------------------------------------------> #
DOCKER_COMPOSE=sudo docker compose                      # <- how local user executes "docker compose" or docker-compose command -----------------------------> #

# CONTAINERS BASE INFORMATION FOR BUILDING WITH docker-compose.yml
PROJECT_NAME="ITX BACKEND EXAM"                         # <- project name will be used for automation outputs -----------------------------------------------> #
PROJECT_LEAD=itx                                        # <- abbreviation or acronym name as part of the container tag that is useful relationship naming ---> #
PROJECT_HOST="127.0.0.1"                                # <- machine hostname referrer - not necessary for this project -------------------------------------> #
PROJECT_CNET=pr-dev                                     # <- useful when a network is required for container connections between each other -----------------> #

# API - LOCAL
APIREST_PLTF=nginx-java-21                              # <- platform assets directory's name ---------------------------------------------------------------> #
APIREST_IMGK=alpine3.23-nginx-java21                    # <- real main image keys to manage automations for sharing resources -------------------------------> #
APIREST_PORT=6500                                       # <- local machine port opened for container service ------------------------------------------------> #
APIREST_BIND="../../../apirest"                         # <- path where application is binded from container to local ---------------------------------------> #
APIREST_CAAS=pr-api-dev                                 # <- container name to build the service - it is important to set the environment in this variable --> #
APIREST_CAAS_USER=cosuser                               # <- container's project directory user if it is required -------------------------------------------> #
APIREST_CAAS_GROUP=cosgroup                             # <- container's project directory group if it is required ------------------------------------------> #
APIREST_CAAS_CPUS=2.00                                  # <- container's maximum CPU usage according to remote infrastructure -------------------------------> #
APIREST_CAAS_MEM=512M                                   # <- container's maximum RAM usage according to remote infrastructure -------------------------------> #
APIREST_CAAS_SWAP=1G                                    # <- container's RAM swap space in storage executed by automation command (docker swarm) ------------> #

# GRAFANA
GRAFANA_PLTF=grafana-8.1                                # <- platform assets directory's name ---------------------------------------------------------------> #
GRAFANA_IMGK=grafana/grafana:8.1.2                      # <- real main image keys to manage automations for sharing resources -------------------------------> #
GRAFANA_PORT=6501                                       # <- local machine port opened for container service ------------------------------------------------> #
GRAFANA_BIND=""                                         # <- path where application is binded from container to local ---------------------------------------> #
GRAFANA_CAAS=pr-grafana-dev                             # <- container name to build the service - it is important to set the environment in this variable --> #
GRAFANA_CAAS_USER=cosuser                               # <- container's project directory user if it is required -------------------------------------------> #
GRAFANA_CAAS_GROUP=cosgroup                             # <- container's project directory group if it is required ------------------------------------------> #
GRAFANA_CAAS_CPUS=2.00                                  # <- container's maximum CPU usage according to remote infrastructure -------------------------------> #
GRAFANA_CAAS_MEM=128M                                   # <- container's maximum RAM usage according to remote infrastructure -------------------------------> #
GRAFANA_CAAS_SWAP=256M                                  # <- container's RAM swap space in storage executed by automation command (docker swarm) ------------> #

# GRAFANA K6
K6_PLTF=k6-0.28                                         # <- platform assets directory's name ---------------------------------------------------------------> #
K6_IMGK=loadimpact/k6:0.28.0                            # <- real main image keys to manage automations for sharing resources -------------------------------> #
K6_PORT=6502                                            # <- local machine port opened for container service ------------------------------------------------> #
K6_BIND=""                                              # <- path where application is binded from container to local ---------------------------------------> #
K6_CAAS=pr-k6-dev                                       # <- container name to build the service - it is important to set the environment in this variable --> #
K6_CAAS_USER=cosuser                                    # <- container's project directory user if it is required -------------------------------------------> #
K6_CAAS_GROUP=cosgroup                                  # <- container's project directory group if it is required ------------------------------------------> #
K6_CAAS_CPUS=2.00                                       # <- container's maximum CPU usage according to remote infrastructure -------------------------------> #
K6_CAAS_MEM=128M                                        # <- container's maximum RAM usage according to remote infrastructure -------------------------------> #
K6_CAAS_SWAP=256M                                       # <- container's RAM swap space in storage executed by automation command (docker swarm) ------------> #

# INFLUXDB
INFLUXDB_PLTF=influxdb-1.8                              # <- platform assets directory's name ---------------------------------------------------------------> #
INFLUXDB_IMGK=influxdb:1.8.2                            # <- real main image keys to manage automations for sharing resources -------------------------------> #
INFLUXDB_PORT=6503                                      # <- local machine port opened for container service ------------------------------------------------> #
INFLUXDB_BIND=""                                        # <- path where application is binded from container to local ---------------------------------------> #
INFLUXDB_CAAS=pr-influxdb-dev                           # <- container name to build the service - it is important to set the environment in this variable --> #
INFLUXDB_CAAS_USER=cosuser                              # <- container's project directory user if it is required -------------------------------------------> #
INFLUXDB_CAAS_GROUP=cosgroup                            # <- container's project directory group if it is required ------------------------------------------> #
INFLUXDB_CAAS_CPUS=2.00                                 # <- container's maximum CPU usage according to remote infrastructure -------------------------------> #
INFLUXDB_CAAS_MEM=128M                                  # <- container's maximum RAM usage according to remote infrastructure -------------------------------> #
INFLUXDB_CAAS_SWAP=256M                                 # <- container's RAM swap space in storage executed by automation command (docker swarm) ------------> #

# SIMULADO
INFLUXDB_PLTF=simulado                                  # <- platform assets directory's name ---------------------------------------------------------------> #
INFLUXDB_IMGK=ldabiralai/simulado:latest                # <- real main image keys to manage automations for sharing resources -------------------------------> #
INFLUXDB_PORT=6504                                      # <- local machine port opened for container service ------------------------------------------------> #
INFLUXDB_BIND=""                                        # <- path where application is binded from container to local ---------------------------------------> #
INFLUXDB_CAAS=pr-simulado-dev                           # <- container name to build the service - it is important to set the environment in this variable --> #
INFLUXDB_CAAS_USER=cosuser                              # <- container's project directory user if it is required -------------------------------------------> #
INFLUXDB_CAAS_GROUP=cosgroup                            # <- container's project directory group if it is required ------------------------------------------> #
INFLUXDB_CAAS_CPUS=2.00                                 # <- container's maximum CPU usage according to remote infrastructure -------------------------------> #
INFLUXDB_CAAS_MEM=128M                                  # <- container's maximum RAM usage according to remote infrastructure -------------------------------> #
INFLUXDB_CAAS_SWAP=256M                                 # <- container's RAM swap space in storage executed by automation command (docker swarm) ------------> #
